import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.palaceresorts.palaceapp',
  appName: 'base-project-pwa',
  webDir: 'dist'
};

export default config;
